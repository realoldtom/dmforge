<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>DMForge Spell Card Sheet</title>
  <link rel="stylesheet" href="{{ css_path }}">
</head>
<body>
  {% for card in cards %}
    <div class="card">
      <div class="header">
        <span>{{ card.title }}</span>
        <span>{{ card.level }} • {{ card.school }}</span>
      </div>
      <div class="desc">{{ card.description }}</div>
      <div class="stats">
        <span>{{ card.casting_time }}</span>
        <span>{{ card.duration }}</span>
        <span>{{ card.components | join(', ') }}</span>
      </div>
    </div>
  {% endfor %}
</body>
</html>
