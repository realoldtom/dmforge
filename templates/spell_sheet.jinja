<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>DMForge Spell Cards</title>
  <link rel="stylesheet" href="{{ css_path }}">
  <style>
    /* Override styles for images to ensure they display properly */
    .image {
      position: relative;
      width: 100%;
      height: 160px;
      margin-bottom: 10px;
      overflow: hidden;
    }
    .image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    /* Stats */
    .stats-row {
        display: flex;
        justify-content: space-between;
        font-size: 0.72rem;
        margin: 0.3rem 0;
        gap: 0.2rem;
        flex-wrap: wrap;
    }

    .stats-row span {
        flex: 1;
        text-align: center;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    /* Description */
    .desc {
        font-size: 0.72rem;
        line-height: 1.15;
        overflow: hidden;
        text-overflow: ellipsis;
        display: -webkit-box;
        -webkit-line-clamp: 7;
        -webkit-box-orient: vertical;
        padding-top: 0.3rem;
        flex: 1;
    }
  </style>
</head>
<body>
  {% for card in cards %}
  <div class="card">
    <div class="title-line">
      <span class="title">{{ card.title }}</span>
      <span class="level_school">{{ card.level }} • {{ card.school }}</span>
    </div>

      <!-- Art with new frame ------------------------- -->
        <div class="image-frame">
            <img src="{{ card.art_url or default_image }}"
                alt=""
                class="image">
        </div>

      <!-- Stats strip in its own framed box ---------- -->
      <div class="stats-frame">
        <div class="stats-strip">
          <span>{{ card.casting_time }}</span>
          <span>{{ card.range }}</span>
          <span>{{ card.duration_short }}</span>
          <span>{{ card.components_short }}</span>
        </div>
      </div>

    <div class="desc-frame">
    <p class="desc">{{ card.description }}</p>
    </div>
    
  </div>
  {% endfor %}
</body>
</html>