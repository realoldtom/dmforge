<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>DMForge Spell Cards</title>
  <link rel="stylesheet" href="{{ css_path }}">
  <style>
    /* Override styles for images to ensure they display properly */
    .image {
      position: relative;
      width: 100%;
      height: 180px;
      overflow: hidden;
    }
    .image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  </style>
</head>
<body>
  {% for card in cards %}
  <div class="card">
    <div class="title-line">
      <span class="title">{{ card.title }}</span>
      <span class="level-school">{{ card.level }} • {{ card.school }}</span>
    </div>

    <!-- Direct image tag instead of background-image -->
    <div class="image">
      <img src="{{ card.art_url or default_image }}" alt="{{ card.title }}">
    </div>

    <div class="stats-row">
      <span>{{ card.casting_time }}</span>
      <span>{{ card.range }}</span>
      <span>{{ card.duration }}</span>
      <span>{{ card.components | join(", ") }}</span>
    </div>

    <div class="desc">{{ card.description }}</div>
  </div>
  {% endfor %}
</body>
</html>