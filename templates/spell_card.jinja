<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>DMForge Spell Cards</title>
  <link rel="stylesheet" href="{{ css_path }}">
</head>
<body>
  {% for card in cards %}
  <div class="card">
    <div class="header">
      <div class="title">{{ card.title }}</div>
      <div class="level">● {{ card.level }} {{ card.school }}</div>
    </div>

    <div class="image"
      style="background-image: url('https://placekitten.com/300/180');">
      <!-- Replace with card.art_url or similar in future -->
    </div>

    <div class="stats">
      <div>{{ card.casting_time }}</div>
      <div>{{ card.duration }}</div>
      <div>{{ card.components | join(', ') }}</div>
    </div>

    <div class="desc">{{ card.description }}</div>
  </div>
  {% endfor %}
</body>
</html>
